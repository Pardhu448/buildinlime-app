import { SlidersHorizontal } from "lucide-react";
import { ToolbarButton } from "./ToolbarButton";

export function FilterButton() {
  return <ToolbarButton icon={SlidersHorizontal} label="Filter" />;
}
